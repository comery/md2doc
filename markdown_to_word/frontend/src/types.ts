
export interface EditorConfig {
  fontFamily: {
    english: string;
    chinese: string;
  };
  fontSize: {
    h1: number;
    h2: number;
    h3: number;
    body: number;
  };
  colors: {
    markdownBg: string;
    wordBg: string;
  };
}